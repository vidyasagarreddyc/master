name: Main Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/greet.yml
    with:
      name: 'World'
      greeting: 'Hello'
